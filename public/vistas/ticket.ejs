<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ticket</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="\css\ticket.css" />
</head>
<body>
    <header>
      <h1>LO ESPERAMOS NUEVAMENTE TIENDA TECH</h1>
    </header>
    <nav>
      <ul class="contenedor">
        <li><img src="../images/logo.png" alt="Logo-empresa" class="nav-icon"/></li>
        <div class="texto">
          <li><a href="/">Inicio</a></li>
          <li><a href="/productos">Productos</a></li>
        </div>
      </ul>
    </nav>

    <p>ID: <%= ticket.id %></p>
    <p>Fecha:<%= ticket.fecha %></p>
    <div>
      <p>Gracias por su compra <%= ticket.nombre %></p>
      <% ticket.productos.forEach(e => { %>
        <p>  Producto: <%= e.nombre %>  |  Precio Unitario: $<%= e.precio.toFixed(2) %>  |  Cantidad: <%= e.cantidad %>  |  Sub-Total: $<%= (e.cantidad * e.precio).toFixed(2) %></p>
        <% }); 
        
      %>
    </div>
    <p class="total">Total: $<%= parseFloat(ticket.total).toFixed(2) %></p>
    <% if(!!ticket.view) { %>
    <a href="http://localhost:3000/ticket/download/<%= ticket.id %>" target="_blank">
      <button type="button" class="btn btn-info btn-sm">
        Descargar ticket
      </button>
    </a>
  <% } %>
</body>

<footer>
    <div>
      <div class="row">
        <div class="col-sm-2">Síguenos en: </div>
        <div class="col-sm-1"><a href="https://twitter.com/inicio" target="_blank">Twitter</a></div>
        <div class="col-sm-1"><a href="https://facebook.com/inicio" target="_blank">Facebook</a></div>
        <div class="col-sm-3">Carlos Rial  &  Lucas Amarilla </a></div>
        <div class="col-sm-5">©2025 TP Programacion 3. Todos los derechos reservados</div>
      </div>
    </div>
  </footer>

</html>